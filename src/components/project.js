import React from "react";

export default function Project(){
    return(
        <section id="project" style={{minHeight:800}}>
            <h1 className="text-shaded fs-1">Experience:</h1>
        </section>
    );
}